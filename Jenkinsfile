pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo "build in progress"
            }
        }

        stage('test') {
            steps {
                echo "test in progress"
            }
        }

        stage('deploy') {
            steps {
                echo "deploy in progress"
            }
        }
    }
}
